name: movie_explorer
description: "A Flutter movie explorer app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.3.0
  provider: ^6.1.2
  sqflite: ^2.4.0
  path_provider: ^2.1.3
  cached_network_image: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true